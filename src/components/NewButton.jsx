import React from 'react'

const NewButton = () => {
    return (
        <div>
            <button type="button" class="btn btn-warning">New</button>
        </div>
    )
}

export default NewButton
